# LeetCode 283 - 移动零
`https://leetcode.cn/problems/move-zeroes/?envType=study-plan-v2&envId=programming-skills`

给定一个数组 nums，编写一个函数将所有 0 移动到数组的末尾，同时保持非零元素的相对顺序。

请注意 ，必须在不复制数组的情况下原地对数组进行操作。

## 思路

a.遍历，计算0的个数，再erase remove掉原本的0，再把去掉的零加到最后面。

b.优化后，使用快慢指针，更快。

## 标签

- 双指针
- 数组

## 初始代码

```cpp
class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int cnt=0;
        for (int i: nums){
            if (i==0) cnt+=1;
        }
        nums.erase(remove(nums.begin(),nums.end(),0),nums.end());
        for (int i=0; i<cnt; i++){
            nums.push_back(0);
        }
    }
};
```
### 问题

- 1.无明显问题，但是两个for循环和一个remove函数导致常数级复杂度偏高，可以使用快慢指针。
- 2.XXX

### 时/空间复杂度

- 时间复杂度：O(n)
- 空间复杂度：O(1)

## 最终代码

```cpp
class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int slow=0;
        for(int fast=0;fast<nums.size();fast++){
            if(nums[fast]!=0){
                swap(nums[slow],nums[fast]);
                slow++;
            }
        }
    }
};
```

### 改进
- 1.使用了快慢指针，优化了常数级复杂度
- 2.XXX

### 时/空间复杂度

- 时间复杂度：O(n)
- 空间复杂度：O(1)

## 收获

- 1.学习使用快慢指针。
- 2.XXX
- 3.XXX

