# LeetCode 28 - 找出字符串中第一个匹配项的下标

## 思路

使用双指针逐个匹配。

## 踩坑

- `while` 条件是在每轮开始前判断。
- `return` 会立即结束函数。
- 需要注意边界条件。

## 最终代码

```cpp
class Solution {
public:
    int strStr(string haystack, string needle) {
        // ...
    }
};
```

## 收获

- 学会使用 CLion 调试器
- 更理解 `while` 和 `return`
- 熟悉字符串索引
