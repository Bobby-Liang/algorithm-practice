# LeetCode 1481 - 去掉最高最低工资后的工资平均值
`https://leetcode.cn/problems/average-salary-excluding-the-minimum-and-maximum-salary/description/?envType=study-plan-v2&envId=programming-skills`

给你一个整数数组 salary ，数组里每个数都是 唯一 的，其中 salary[i] 是第 i 个员工的工资。
请你返回去掉最低工资和最高工资以后，剩下员工工资的平均值。

## 思路

去掉最高，去掉最低，求平均。

## 标签

- 排序
- 数组

## 初始代码

```cpp
class Solution {
public:
    double average(vector<int>& salary) {
        sort(salary.begin(),salary.end());
        salary.front()=0;
        salary.back()=0;
        double sum = accumulate(salary.begin(),salary.end(),0.0);
        double ans=sum/(salary.size()-2);
        return ans;
    }
};
```
### 问题

- 1.sort和accumulate会消耗额外的空间和时间。
- 2.XXX

### 时/空间复杂度

- 时间复杂度：O(n log n)
- 空间复杂度：O(log n)

## 最终代码

```cpp
class Solution {
public:
    double average(vector<int>& salary) {
        int szs=salary.size();int maxs=0;int mins=1000000;double sums=0.0;
        for (int i:salary){
            if (i>maxs) maxs=i;
            if (i<mins) mins=i;
            sums+=i;
        }
        sums-=maxs;sums-=mins;
        double ans=sums/(szs-2);
        return ans;
    }
};
```

### 改进
- 1.自己写一个for循环，计算max、min和sum，时间空间复杂度都更低
- 2.XXX

### 时/空间复杂度

- 时间复杂度：O(n)
- 空间复杂度：O(1)

## 收获

- 1.有的时候调用标准库不如自己写循环来的省时间空间。
- 2.XXX
- 3.XXX

